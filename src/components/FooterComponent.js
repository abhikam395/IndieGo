import React, { Component } from "react";
import './footer.scss';

export default class FooterComponent extends Component{
    render(){
        return (
            <section>
                FooterComponent
            </section>
        )
    }
}
